receivers:
  otlp:
    protocols:
      grpc:
      http:

exporters:
  spanreportexporter:
    path: "./span_report.txt"
    verbose: true
    report_interval: 1m

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [spanreportexporter]
